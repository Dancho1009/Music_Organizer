[project]
name = "music-organizer-engine"
version = "0.1.0"
description = "Auditable music library migration engine"
requires-python = ">=3.11"
dependencies = ["mutagen>=1.47"]

[tool.pytest.ini_options]
testpaths = ["../../tests/engine"]

